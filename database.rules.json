{
  "rules": {
    "events": {
      "$event": {
        "assignments": {
          ".read": true,
          ".write": true,
          "$id": {
            ".validate": "newData.hasChildren(['discord','type','ts'])"
          }
        }
      }
    }
  }
}
